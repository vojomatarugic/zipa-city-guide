/**
 * Shared class strings for category listing pages (Events, Clubs, etc.).
 * Canonical layout matches EventsPage section shells.
 */
export const LISTING_PAGE_HERO_SECTION_CLASS =
  "relative w-full min-h-[320px]";

/** Standard content band under the hero (vertical padding + minimum section height). */
export const LISTING_PAGE_CONTENT_SECTION_CLASS =
  "py-16 min-h-[320px]";
